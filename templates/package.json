{
  "name": "<%= name %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "main": "dist/main/index.js",
  "scripts": {
    "precommit": "npm run lint && npm test && npm run coverage",
    "start": "node dist/main",
    "startDev": "nodemon --watch src/main --watch package.json -e ts,json -q -x 'rm -fr dist && echo \"\\n============\\nCOMPILING...\\n============\\n\\n\" && tsc --outDir dist || return 0 && node --debug dist/main/index.js'",
    "make": "rm -fr dist && tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "test": "nyc tape $(find dist -name '*.js' ! -name 'index.js') | tap-spec",
    "coverage": "nyc check-coverage --lines 80 --functions 80 --branches 80"
  },
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "exclude": [
      "dist/main/index.js",
      "**/*.d.ts"
    ]
  },
  "author": "Repositive Team",
  "license": "MIT",
  "keywords": ["<%= name %>", "service"],
  "dependencies": {},
  "devDependencies": {}
}
